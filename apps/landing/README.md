# PARA landing

This app is a standalone landing variant for PARA. It should mirror the same-origin story used by the canonical public site in `apps/docs`, including links into `/docs`.

## Commands

```bash
pnpm dev
pnpm check
pnpm lint
pnpm test
pnpm build
```

Use `apps/docs` for the canonical public site with the full `/docs` reference tree.
